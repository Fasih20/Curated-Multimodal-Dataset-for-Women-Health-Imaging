# Presentation

The 14-slide Beamer deck mirrors the paper and uses an existing compound-figure demo. Its numerical results are loaded from `../paper/tables/results_table.tex`, which must be generated by Stage 11.6 after real evaluation.

Build from this directory with:

```bash
latexmk -pdf slides.tex
```

When the generated table is absent, the deck honestly displays “Results pending.”
